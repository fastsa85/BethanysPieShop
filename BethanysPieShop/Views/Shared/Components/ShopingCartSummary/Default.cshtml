@model ShopingCartViewModel

@if (Model.ShopingCart.ShopingCartItems.Count > 0)
{
    <li>
        <a>
            <span class="glyphicon glyphicon-shopping-cart"></span>
            <span id="cart-status">
                @Model.ShopingCart.ShopingCartItems.Count
            </span>
        </a>
    </li>
}